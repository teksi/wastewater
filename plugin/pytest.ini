# plugin/pytest.ini

[pytest]
addopts = --strict-markers

markers =
    qgis: tests requiring a QGIS runtime
    no_qgis: tests for command-line functionality without QGIS python libraries
    integration: tests requiring dockerised services such as PostgreSQL, QGIS container, ili2pg or PUM setup
